---
name: doc-coauthoring
description: 通过结构化工作流引导用户协作撰写文档。当用户想要撰写文档、提案、技术规格说明、决策文档或类似结构化内容时使用。此工作流帮助用户高效传递上下文、通过迭代完善内容，并验证文档对读者有效。当用户提到写文档、创建提案、起草规格说明或类似文档任务时触发。
---

# 文档协作撰写工作流

本技能提供结构化工作流，引导用户完成协作式文档创建。作为积极引导者，带领用户经历三个阶段：上下文收集、完善与结构、读者测试。

## 何时提供此工作流

**触发条件：**
- 用户提到写文档："write a doc"、"draft a proposal"、"create a spec"、"write up"
- 用户提到特定文档类型："PRD"、"design doc"、"decision doc"、"RFC"
- 用户似乎正在开始一项重要的写作任务

**初始提议：**
向用户提议一个结构化工作流来协作撰写文档。解释三个阶段：

1. **上下文收集**：用户提供所有相关上下文，Claude 提出澄清问题
2. **完善与结构**：通过头脑风暴和编辑迭代构建每个章节
3. **读者测试**：用全新的 Claude（无上下文）测试文档，在他人阅读前发现盲点

解释这种方法有助于确保文档在他人阅读时效果良好（包括当他们将文档粘贴给 Claude 时）。询问他们是否想尝试此工作流，还是更喜欢自由形式。

如果用户拒绝，自由形式工作。如果用户接受，进入阶段一。

## 阶段一：上下文收集

**目标：** 缩小用户所知与 Claude 所知之间的差距，为后续的智能指导奠定基础。

### 初始问题

首先向用户询问关于文档的元上下文：

1. 这是什么类型的文档？（如技术规格说明、决策文档、提案）
2. 主要受众是谁？
3. 希望读者阅读后产生什么影响？
4. 有没有模板或特定格式需要遵循？
5. 还有其他约束或上下文需要了解吗？

告知他们可以用简写或任何方便的方式回答。

**如果用户提供了模板或提到了文档类型：**
- 询问他们是否有模板文档可以分享
- 如果他们提供了共享文档的链接，使用适当的集成来获取
- 如果他们提供了文件，阅读它

**如果用户提到在编辑现有共享文档：**
- 使用适当的集成来读取当前状态
- 检查没有 alt 文本的图片
- 如果存在没有 alt 文本的图片，解释当他人使用 Claude 来理解文档时，Claude 无法看到这些图片。询问他们是否需要生成 alt 文本。如果是，请求他们将每张图片粘贴到聊天中以生成描述性 alt 文本。

### 信息倾倒

初始问题回答后，鼓励用户倾倒所有他们有的上下文。请求以下信息：
- 项目/问题的背景
- 相关的团队讨论或共享文档
- 为什么没有使用其他替代方案
- 组织上下文（团队动态、过去的事件、政治因素）
- 时间线压力或约束
- 技术架构或依赖关系
- 利益相关者的关切

建议他们不用担心组织问题——尽管全部倒出来。提供多种传递上下文的方式：
- 信息倾倒式意识流
- 指向团队频道或讨论串
- 链接到共享文档

**如果有可用的集成**（如 Slack、Teams、Google Drive、SharePoint 或其他 MCP 服务器），提及这些可以用来直接拉取上下文。

**如果没有检测到集成且在 Claude.ai 或 Claude 应用中：** 建议他们可以在 Claude 设置中启用连接器，以允许直接从消息应用和文档存储中拉取上下文。

告知他们完成初始倾倒后会提出澄清问题。

**在上下文收集期间：**

- 如果用户提到团队频道或共享文档：
  - 如果集成可用：告知他们内容将被立即读取，然后使用适当的集成
  - 如果集成不可用：解释缺乏访问权限。建议他们在 Claude 设置中启用连接器，或直接粘贴相关内容

- 如果用户提到未知实体/项目：
  - 询问是否应该搜索已连接的工具以了解更多
  - 等待用户确认后再搜索

- 当用户提供上下文时，记录已学到的东西和仍不清楚的内容

**提出澄清问题：**

当用户表示已完成初始倾倒（或提供了大量上下文后），提出澄清问题以确保理解：

根据上下文中的空白生成 5-10 个编号问题。

告知他们可以用简写回答（如 "1: yes, 2: see #channel, 3: no because backwards compat"）、链接到更多文档、指向要阅读的频道，或继续信息倾倒。最有效的方式即可。

**退出条件：**
当问题显示理解到位时——当可以询问边缘情况和权衡而不需要解释基础知识时，说明已收集到足够上下文。

**过渡：**
询问他们在此阶段是否还有更多上下文要提供，或者是否可以开始起草文档了。

如果用户想添加更多，让他们添加。准备就绪后，进入阶段二。

## 阶段二：完善与结构

**目标：** 通过头脑风暴、筛选和迭代完善，逐章节构建文档。

**给用户的说明：**
解释文档将逐章节构建。每个章节的流程：
1. 提出澄清问题，了解要包含的内容
2. 头脑风暴出 5-20 个选项
3. 用户指示保留/删除/合并哪些
4. 起草章节
5. 通过精细编辑完善

从未知最多的章节开始（通常是核心决策/提案），然后处理其余部分。

**章节排序：**

如果文档结构清晰：
询问他们想从哪个章节开始。

建议从未知最多的章节开始。对于决策文档，这通常是核心提案。对于规格说明，通常是技术方法。摘要章节最好留到最后。

如果用户不知道需要哪些章节：
根据文档类型和模板，建议适合该文档类型的 3-5 个章节。

询问这个结构是否合适，或者是否要调整。

**结构确定后：**

创建包含所有章节占位符文本的初始文档结构。

**如果有 artifacts 访问权限：**
使用 `create_file` 创建 artifact。这为 Claude 和用户双方提供一个可依循的脚手架。

告知他们将创建包含所有章节占位符的初始结构。

创建 artifact，包含所有章节标题和简要占位符文本如"[待撰写]"或"[内容在此]"。

提供 artifact 链接，并表示现在是时候填充每个章节了。

**如果没有 artifacts 访问权限：**
在工作目录中创建一个 markdown 文件。适当命名（如 `decision-doc.md`、`technical-spec.md`）。

告知他们将创建包含所有章节占位符的初始结构。

创建包含所有章节标题和占位符文本的文件。

确认文件名已创建，并表示现在是时候填充每个章节了。

**对于每个章节：**

### 步骤一：澄清问题

宣布开始处理 [章节名称] 章节。提出 5-10 个关于应包含内容的澄清问题：

根据上下文和章节目的生成 5-10 个具体问题。

告知他们可以用简写回答，或指出哪些内容重要。

### 步骤二：头脑风暴

对于 [章节名称] 章节，头脑风暴 5-20 个可能包含的内容，视章节复杂程度而定。寻找：
- 可能被遗忘的已分享上下文
- 尚未提到的角度或考虑因素

根据章节复杂程度生成 5-20 个编号选项。最后，如果他们想要更多选项，提供更多头脑风暴。

### 步骤三：筛选

询问哪些要点应该保留、删除或合并。请求简要理由以帮助了解后续章节的优先级。

提供示例：
- "保留 1,4,7,9"
- "删除 3（与 1 重复）"
- "删除 6（读者已知道）"
- "合并 11 和 12"

**如果用户给出自由形式反馈**（如"看起来不错"或"我喜欢大部分但..."）而不是编号选择，提取他们的偏好并继续。解析他们想要保留/删除/修改的内容并应用。

### 步骤四：空白检查

根据他们选择的内容，询问 [章节名称] 章节是否有重要遗漏。

### 步骤五：起草

使用 `str_replace` 将该章节的占位符文本替换为实际起草的内容。

宣布将根据他们选择的内容起草 [章节名称] 章节。

**如果使用 artifacts：**
起草后，提供 artifact 链接。

请他们阅读并指出要修改的地方。注意具体说明有助于为后续章节学习风格。

**如果使用文件（无 artifacts）：**
起草后，确认完成。

告知他们 [章节名称] 章节已在 [文件名] 中起草。请他们阅读并指出要修改的地方。注意具体说明有助于为后续章节学习风格。

**给用户的重要说明（起草第一个章节时包含）：**
提供一个提示：不要直接编辑文档，而是让他们指出要修改的内容。这有助于学习他们的风格以便用于后续章节。例如："删除 X 要点——已被 Y 覆盖"或"让第三段更简洁"。

### 步骤六：迭代完善

当用户提供反馈时：
- 使用 `str_replace` 进行编辑（绝不重印整个文档）
- **如果使用 artifacts：** 每次编辑后提供 artifact 链接
- **如果使用文件：** 仅确认编辑完成
- 如果用户直接编辑文档并要求阅读：心理记录他们所做的更改，并在后续章节中记住（这显示他们的偏好）

**继续迭代** 直到用户对章节满意。

### 质量检查

连续 3 次迭代没有实质性更改后，询问是否可以删除任何不影响重要信息的内容。

章节完成时，确认 [章节名称] 已完成。询问是否准备进入下一章节。

**对所有章节重复。**

### 接近完成

当完成度达到 80% 以上时，宣布意图重读整个文档并检查：
- 章节间的流畅性和一致性
- 冗余或矛盾
- 任何看起来像"敷衍"或通用填充物的地方
- 每个句子是否都有分量

阅读整个文档并提供反馈。

**当所有章节都已起草和完善后：**
宣布所有章节已起草。表示意图再审查一次完整文档。

审查整体连贯性、流畅性和完整性。

提供任何最终建议。

询问是否准备进入读者测试，或者是否还有其他内容要完善。

## 阶段三：读者测试

**目标：** 用全新的 Claude（无上下文渗透）测试文档，验证它对读者真正有效。

**给用户的说明：**
解释现在将进行测试，看看文档对读者是否真正有效。这可以发现盲点——对作者有意义但可能让其他人困惑的内容。

### 测试方法

**如果有子代理访问权限（如在 Claude Code 中）：**

直接进行测试，无需用户参与。

### 步骤一：预测读者问题

宣布意图预测读者在尝试发现此文档时可能提出的问题。

生成 5-10 个读者会现实提出的问题。

### 步骤二：用子代理测试

宣布将用全新的 Claude 实例（无此对话的上下文）测试这些问题。

对于每个问题，使用文档内容和问题调用子代理。

总结 Reader Claude 每个问题答对/答错的情况。

### 步骤三：运行额外检查

宣布将执行额外检查。

调用子代理检查歧义、错误假设和矛盾。

总结发现的问题。

### 步骤四：报告并修复

如果发现问题：
报告 Reader Claude 在特定问题上遇到了困难。

列出具体问题。

表示意图修复这些空白。

回到有问题的章节进行完善。

---

**如果没有子代理访问权限（如 claude.ai 网页界面）：**

用户需要手动进行测试。

### 步骤一：预测读者问题

询问人们在尝试发现此文档时可能提出什么问题。他们会在 Claude.ai 中输入什么？

生成 5-10 个读者会现实提出的问题。

### 步骤二：设置测试

提供测试说明：
1. 打开一个新的 Claude 对话：https://claude.ai
2. 粘贴或分享文档内容（如果使用启用了连接器的共享文档平台，提供链接）
3. 向 Reader Claude 提问生成的问题

对于每个问题，指导 Reader Claude 提供：
- 答案
- 是否有任何歧义或不清楚的地方
- 文档假设读者已知的知识和上下文

检查 Reader Claude 是否给出正确答案或误解了任何内容。

### 步骤三：额外检查

还向 Reader Claude 询问：
- "文档中哪些内容对读者可能歧义或不清楚？"
- "文档假设读者已经有哪些知识或上下文？"
- "是否存在任何内部矛盾或不一致？"

### 步骤四：根据结果迭代

询问 Reader Claude 答错或遇到困难的地方。表示意图修复这些空白。

对于有问题的章节回到完善阶段。

---

### 退出条件（两种方法）

当 Reader Claude 一致地正确回答问题且没有发现新的空白或歧义时，文档就绪。

## 最终审查

当读者测试通过时：
宣布文档已通过 Reader Claude 测试。完成前：

1. 建议他们自己再读一遍——他们拥有这份文档并对其质量负责
2. 建议核实任何事实、链接或技术细节
3. 请他们验证文档是否达到了预期的效果

询问他们是否想要再审查一次，或者工作是否完成。

**如果用户想要最终审查，提供它。否则：**
宣布文档完成。提供一些最终提示：
- 考虑在附录中链接此对话，以便读者可以看到文档是如何开发的
- 使用附录提供深度而不使主文档臃肿
- 当从实际读者那里收到反馈时更新文档

## 有效指导的提示

**语气：**
- 直接且程序化
- 当影响用户行为时简要解释理由
- 不要试图"推销"方法——直接执行

**处理偏离：**
- 如果用户想跳过某个阶段：询问他们是否要跳过并自由形式写作
- 如果用户看起来沮丧：承认这比预期花费更长时间。提供加快速度的方法
- 始终给用户调整流程的主动权

**上下文管理：**
- 始终如一，如果提到的内容缺少上下文，主动询问
- 不要让空白累积——出现时就解决

**Artifact 管理：**
- 使用 `create_file` 起草完整章节
- 使用 `str_replace` 进行所有编辑
- 每次更改后提供 artifact 链接
- 头脑风暴列表不要使用 artifacts——那只是对话

**质量优先于速度：**
- 不要匆忙走过阶段
- 每次迭代都应该带来有意义的改进
- 目标是创建对读者真正有效的文档

（文件结束 - 共 375 行）
